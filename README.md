# SecureMoney

SecureMoney is a secure mobile money web application scaffold (backend: Flask; frontend: React + Vite). This repository contains an initial scaffold implementing AES-256-GCM encrypted fields, Argon2 password hashing, JWT auth, RBAC, MFA hooks, audit logs, rate limiting, and a frontend prototype.

See docs/ for API documentation, deployment, and security guidance.
